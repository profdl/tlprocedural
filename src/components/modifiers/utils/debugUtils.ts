/**
 * Debug logging utility
 */
export function logShapeOperation(
  operation: string,
  shapeId: string,
  details: Record<string, unknown>
): void {
  // Debug logging disabled - uncomment the line below to re-enable when needed
  // console.log(`[${operation}] Shape ${shapeId}:`, details)
} 